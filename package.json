{
  "name": "google-login-app",
  "version": "1.0.0",
  "description": "A simple web application with Google login functionality",
  "main": "server/index.js",
  "scripts": {
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "server": "cd server && python -m uvicorn main:app --reload --port 8000",
    "client": "cd client && npm start",
    "build": "cd client && npm run build",
    "install-all": "npm install && cd client && npm install && cd ../server && pip install -r requirements.txt"
  },
  "keywords": ["google-login", "oauth", "react", "fastapi"],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
} 